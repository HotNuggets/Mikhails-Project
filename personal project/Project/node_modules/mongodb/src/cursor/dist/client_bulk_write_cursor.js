"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
exports.__esModule = true;
exports.ClientBulkWriteCursor = void 0;
var client_bulk_write_1 = require("../operations/client_bulk_write/client_bulk_write");
var execute_operation_1 = require("../operations/execute_operation");
var utils_1 = require("../utils");
var abstract_cursor_1 = require("./abstract_cursor");
/**
 * This is the cursor that handles client bulk write operations. Note this is never
 * exposed directly to the user and is always immediately exhausted.
 * @internal
 */
var ClientBulkWriteCursor = /** @class */ (function (_super) {
    __extends(ClientBulkWriteCursor, _super);
    /** @internal */
    function ClientBulkWriteCursor(client, commandBuilder, options) {
        if (options === void 0) { options = {}; }
        var _this = _super.call(this, client, new utils_1.MongoDBNamespace('admin', '$cmd'), options) || this;
        _this.commandBuilder = commandBuilder;
        _this.clientBulkWriteOptions = options;
        return _this;
    }
    Object.defineProperty(ClientBulkWriteCursor.prototype, "response", {
        /**
         * We need a way to get the top level cursor response fields for
         * generating the bulk write result, so we expose this here.
         */
        get: function () {
            if (this.cursorResponse)
                return this.cursorResponse;
            return null;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(ClientBulkWriteCursor.prototype, "operations", {
        get: function () {
            return this.commandBuilder.lastOperations;
        },
        enumerable: false,
        configurable: true
    });
    ClientBulkWriteCursor.prototype.clone = function () {
        var clonedOptions = utils_1.mergeOptions({}, this.clientBulkWriteOptions);
        delete clonedOptions.session;
        return new ClientBulkWriteCursor(this.client, this.commandBuilder, __assign({}, clonedOptions));
    };
    /** @internal */
    ClientBulkWriteCursor.prototype._initialize = function (session) {
        return __awaiter(this, void 0, Promise, function () {
            var clientBulkWriteOperation, response;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        clientBulkWriteOperation = new client_bulk_write_1.ClientBulkWriteOperation(this.commandBuilder, __assign(__assign(__assign({}, this.clientBulkWriteOptions), this.cursorOptions), { session: session }));
                        return [4 /*yield*/, execute_operation_1.executeOperation(this.client, clientBulkWriteOperation)];
                    case 1:
                        response = _a.sent();
                        this.cursorResponse = response;
                        return [2 /*return*/, { server: clientBulkWriteOperation.server, session: session, response: response }];
                }
            });
        });
    };
    return ClientBulkWriteCursor;
}(abstract_cursor_1.AbstractCursor));
exports.ClientBulkWriteCursor = ClientBulkWriteCursor;
