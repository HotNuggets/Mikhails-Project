"use strict";
exports.__esModule = true;
exports.Explain = exports.ExplainVerbosity = void 0;
/** @public */
exports.ExplainVerbosity = Object.freeze({
    queryPlanner: 'queryPlanner',
    queryPlannerExtended: 'queryPlannerExtended',
    executionStats: 'executionStats',
    allPlansExecution: 'allPlansExecution'
});
/** @internal */
var Explain = /** @class */ (function () {
    function Explain(verbosity, maxTimeMS) {
        if (typeof verbosity === 'boolean') {
            this.verbosity = verbosity
                ? exports.ExplainVerbosity.allPlansExecution
                : exports.ExplainVerbosity.queryPlanner;
        }
        else {
            this.verbosity = verbosity;
        }
        this.maxTimeMS = maxTimeMS;
    }
    Explain.fromOptions = function (_a) {
        var explain = (_a === void 0 ? {} : _a).explain;
        if (explain == null)
            return;
        if (typeof explain === 'boolean' || typeof explain === 'string') {
            return new Explain(explain);
        }
        var verbosity = explain.verbosity, maxTimeMS = explain.maxTimeMS;
        return new Explain(verbosity, maxTimeMS);
    };
    return Explain;
}());
exports.Explain = Explain;
