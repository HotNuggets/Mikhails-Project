'use strict';
/**
 * Returns `true` if the given index options have a `text` option.
 */

module.exports = function isTextIndex(indexKeys) {
  var isTextIndex = false;

  for (var _i = 0, _Object$keys = Object.keys(indexKeys); _i < _Object$keys.length; _i++) {
    var key = _Object$keys[_i];

    if (indexKeys[key] === 'text') {
      isTextIndex = true;
    }
  }

  return isTextIndex;
};